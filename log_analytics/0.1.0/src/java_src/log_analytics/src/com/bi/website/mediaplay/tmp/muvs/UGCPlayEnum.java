package com.bi.website.mediaplay.tmp.muvs;

public enum UGCPlayEnum {
/**
 *     上报时间(rt)：unix时间戳 ( iphone) 
   用户ip (IP)： 
   Mac地址
   用户id (UID)：
   FPC(策略，地区，运营商)：例：0_0_z1
   UGC_ID
   Infohash_id (种子ID)
     FCK
   URL

 */
    TIMESTAMP,IP,MAC,UID,FPC,UGC_ID,INFORHASH_ID,FCK,URL;
}
